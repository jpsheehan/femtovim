{
  "scripts": {
    "test": "mocha --require ts-node/register --extension ['ts'] ./src/**/*.test.ts",
    "test:watch": "mocha --require ts-node/register --watch --watch-files ./src/**/*.ts --extension ['ts'] ./src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "chai": "^4.3.4",
    "mocha": "^9.1.4",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  }
}
